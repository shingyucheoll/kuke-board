/*
 * Comment Service - 댓글 관리 서비스
 *
 * 포트: 9001
 * 역할: 댓글 생성, 수정, 삭제, 조회
 */

plugins {
    id 'service-conventions'  // 마이크로서비스 공통 설정 적용
}

// comment 서비스만의 특별한 의존성이 필요한 경우 여기에 추가
dependencies {
    implementation project(':common:snowflake')  // 댓글 ID 생성 필요시
}
